{
    "name": "serverless-aggregator",
    "description": "NodeJS Aggregator to be run on Lambda",
    "version": "0.0.1",
    "private": true,
    "devDependencies": {
        "@types/aws-lambda": "^8.10.15",
        "mocha": "5.0.0",
        "unit.js": "2.0.0"
    },
    "dependencies": {
        "aws-lambda-http-server": "^0.1.12",
        "gsg-typeix": "^2.0.4"
    }, 
    "scripts": {
        "test": "mocha --recursive tests"
    }
}
