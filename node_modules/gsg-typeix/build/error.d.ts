import { Status } from "./server/status-code";
/**
 * @since 1.0.0
 * @class
 * @name HttpException
 * @param {Number} code status code
 * @param {String} message
 * @param {Object} data
 * @constructor
 * @description
 * HttpException use it in endpoint actions
 */
export declare class HttpError extends Error {
    private code;
    static merge(error: Error): Error;
    constructor(code: Status | number, message?: string, data?: Object);
    getMessage(): string;
    getCode(): Status;
    toString(): string;
}
